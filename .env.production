# Loaded by `npm run build` (mode = production) — this is what Vercel deploys.
# Points the frontend at the deployed backend.
# A Vercel dashboard env var of the same name will override this if set.
VITE_API_URL="https://secure-study-hub-backend.vercel.app"
